@model List<OdeToFood.Models.RestaurantListViewModel>

@{
    ViewBag.Title = "Home Page";
}


@foreach (var item in Model)
{
<div>
    <h4>@item.Name</h4>
    <div>@item.City</div>
    <div>@item.Country</div>
    <div>Reviews: @item.CountOfReviews</div>
    <hr>
</div>
}
